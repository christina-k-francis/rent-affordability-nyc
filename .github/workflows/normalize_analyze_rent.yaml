name: Update and Normalize Rent Tables, and Calculate YoY Change Percent Tables in BQ 

on:
  push:
    branches:
      - main
  
  # Allow manual triggering from GitHub UI
  workflow_dispatch:

jobs:
  Post-ETL-SQL-Queries:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
            
      - name: Authenticate Google Cloud Connection
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCS_SERVICE_ACCOUNT_KEY }}
      
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v1
        with:
          project_id: rent-affordability
          install_components: bq

      - name: Verify authentication
        run: gcloud auth list

      - name: Create reference tables for foreign key connections
        run: |
            set -e
            bq query \
                --project_id=rent_affordability \
                --use_legacy_sql=false \
                < BigQuery/SQL/bigquery_init_reference_tables.sql 

      - name: Execute normalization script
        run: |
            set -e
            bq query \
                --project_id=rent-affordability \
                --use_legacy_sql=false \
                < BigQuery/SQL/normalize_median_rent.sql

      - name: Execute YoY analysis script
        run: |
            set -e
            bq query \
                --project_id=rent-affordability \
                --use_legacy_sql=false \
                < BigQuery/SQL/calc_yoy_median_rent.sql